<h1>All of Tasks</h1>

<% @tasks.each do |task| %>
  <h2><%= task.name %></h2>
  <p><%= task.description %></p>
  <p><%= task.done %></p>
<%= link_to "Show it", task_path(task) %>
<%= link_to "Edit it", edit_task_path(task) %>





<% end %>

<br>
<hr>

<%= link_to "Add a task", new_task_path %>
